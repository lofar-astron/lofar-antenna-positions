language: python
python:
  - "2.7"
  - "3.6"
  - "3.7"
install:
  - pip install pathlib
  - python setup.py install
script:
  - cd .. && pytest --doctest-modules --ignore=lofar-antenna-positions/scripts lofar-antenna-positions
